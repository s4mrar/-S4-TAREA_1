Algoritmo verificar_docena
    // Verifica si un número está entre 1 y 12 primeros 12//
    Definir numero Como Entero
    Escribir "Ingrese un número natural:"
    Leer numero
    Si numero >= 1 Y numero <= 12 Entonces
        Escribir "El número está dentro de la primera docena de números naturales."
    Sino
        Escribir "El número está fuera de la primera docena."
    FinSi
FinAlgoritmo
